package cron

// type rpcServerAdapter struct {
// }

// func (h *rpcServerAdapter) Resolve(c string, conf conf.IMainConf, log *logger.Logger) (servers.IRegistryServer, error) {
// 	return NewCronResponsiveServer(c, conf, log)
// }

// func init() {
// 	servers.Register("cron", &rpcServerAdapter{})
// }
